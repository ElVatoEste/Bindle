// SPDX-License-Identifier: GPL-3.0-or-later

// Package registry fetches and publishes module artifacts (SAVF + metadata)
// against a backend: an IFS directory, a SAVF host, or an S3-compatible bucket.
package registry